{
  "name": "frankmd-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Convenience wrapper for the Tauri CLI. The app itself is Rust (src-tauri/).",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon ../public/icon.svg -o src-tauri/icons"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
